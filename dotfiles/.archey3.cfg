[core]
color = blue
align = center
display_modules = distro(), uname(n), uname(r), uptime(), wm(), de(), packages(), ram(), fs(/), fs(/incoming), fs(/mnt/data), fs(/var/lib/lxc), fs(/scratch)

[fs]
low_bound = 75
medium_bound = 90
